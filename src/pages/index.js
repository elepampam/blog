import React from "react"

export default ({data}) => {    
    return(
        <div>
            halo
        </div>
    )
}


export const query = graphql`
    query IndexPagesQuery{
        
    }
`